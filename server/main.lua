RegisterCommand("poll", function(source, args)
    if source == 0 then
        --TODO: Make the command executable from the server console
        print("^2Command can't be used from the server console for the moment.")
    else

    end
end, true)

-- add_ace group.admin command.poll